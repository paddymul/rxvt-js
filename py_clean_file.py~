#!/usr/bin/python

import re

def one_line_functions(lines):


    function_def=re.compile(r'^([a-zA-Z_]+[:space:]*)$^[:space:]*(rxvt_term::[a-zA-Z_]+ ?\(.*\)).*',re.MULTILINE)


    #gsed -r '/^[a-zA-Z_]+[:space:]**$/ {
    #N
    #s/([a-zA-Z_]+)[:space:]*\n[:space:]*(rxvt_term::[a-zA-Z_]+ ?\(.*\)).*/\1 \2/
    #}'
