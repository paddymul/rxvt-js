
#include "rxvt.h.js"


//          while (row > top_row && R_OW (row - 1).is_longer ())
          while (row > top_row && ROW (row - 1).is_longer ())

              //R_OW(arg1)
              ROW(arg1)

                  //ROW_of(foo,bar)
ROW_of(foo,bar)

                  //LINENO_of(baz,bof)
LINENO_of(baz,bof)